/** Canonical dashboard route for operator open-slot detail (`app/(dashboard)/open-slots/[id]/page.tsx`). */
export function openSlotDetailPath(openSlotId: string): string {
  return `/open-slots/${openSlotId}`;
}
